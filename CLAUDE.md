# このリポジトリで守ること

番組「今日はここに」の制作環境。〒や町名から、その町の碑・遺構・地形の話を集めて
地理院タイルと VOICEVOX で動画にする。**人が読む手順と失敗の実例は
`docs/claude.md`。**ここは毎回守る規則だけを置く。

## 合成の前に機械で止める

```
python tools/readings.py check <ep_dir>
```

**誤読の疑いが1件でもあれば終了コード1で落ちる。必ず通す。**
一度直した誤読を次の回で出したことが実際にあるので、注意力に頼らない。

`suspects`（語を総ざらい）と `scan`（行ごとの読み）は**人が読むためのもの**。
`scan` の出力を目で追うだけでは落ちる。第005回では 六百石→ロッピャッセキ と
墓所→ハカショ が scan にそのまま出ていたのに見落とし、持ち主が音を聞いて
拾った。**数える語（石・貫・俵）と墓・寺の用語は声に出して確かめる。**

## 断定しない

事実と年月日を置く。どちらが正しいかは言わない。これまで扱った題材:

- 水害の死者（第002・004回）— 数字はそのまま出し、地の文で情に寄せない
- 逸話の真偽（第003回「ニイタカヤマノボレ」）
- 開門をめぐる相反判決（第004回）— どちらの側にも立たず、決まったことは決まったと書く
- 棄教したかどうか（第005回）— 調査した側も断定していない。番組も断定しない

出土品などは**報告書の語をそのまま使う**（「玉類」「ガラス板片」を「ロザリオ」に
言い換えない）。宗教・政治の当否には立ち入らず、土地と遺構の話に戻す。

## 数字は計算する。伝聞のまま書かない

距離・方角・年数・尺は必ず出す。第005回の初稿では、実際に次を書いていた。

| 書いたもの | 実際 |
|---|---|
| 南へ二十六キロ | **東南東**（緯度差6km・経度差24.5km） |
| 六百メートルほど | **710m** |
| 十九年かかった | **十八年4か月**（2003年12月→2022年4月） |

年表の裏も取る。会話に残っていたメモの「2017年9月に発掘開始」は**第3次**で、
第1次は2014年9月だった。

## 固有名詞は、出す前に衝突を調べる

番組名「まちぶら」を5本作ってから、**長崎市の現行事業「まちぶらプロジェクト」**
（市自身が YouTube に「まちぶら動画」を出している）と衝突していると分かり、
全5本のタイトルカードを作り直した。**名前を提案するときは先に検索する。**

読みも機械で確かめる。`readings.kana()` に通すと次が分かった。

```
ぶら史   -> ブラシ'          歯ブラシに読める
振紀行   -> _フ'/キコオ'     しんきこう と読まれない
よるまち -> ヨル'/マチ'      夜の街＝歓楽街
碑と地形 -> イシブミト'/...  碑はイシブミ（README に書いてある罠）
```

## 数字の写しを2か所に持たない

`docs/sources.md` に「`seamlessphoto` は 14〜18」と書いてあり、それを信じて
`tiles.LAYERS` にもそう書いたため、第004回の俯瞰が**画面の半分が黒くなった**
（実測は z5 でも欠けない）。**タイル層の範囲は `tools/tiles.py` の `LAYERS` が正。**
文書は「正はどこか」を指すだけにする。

タイル層が使えるかは**叩いて確かめる**。層ごとに整備範囲が違い、無い場所は黒く出る。

## 権利

- 写真は **CC0 / PD / CC BY / Attribution だけ**。**CC BY-SA は使わない**
- 立ち絵は既定で出さない（`--chara` のときだけ）。元データは二次配布禁止
- VOICEVOX は**キャラごとに規約が違う**。`docs/voicevox-characters.md` を見る
- 「このツールでこんな動画が作れます」という見せ方は**宣伝手段としての利用**に
  あたりうる。番組として紹介する形に留める
- 画像はファイル名を信じない。**開いて中身を見る**

## 作業のときの注意

- **ビルドの結果をパイプに通さない。**`python tools/build.py ... | tail` にすると
  終了コードが `tail` のものになり、`set -e` が素通りする。実際に `NameError` を
  見逃してステールな mp4 を作った
- **heredoc にバックスラッシュを書かない。**`\n` が実際の改行に潰れて構文エラーに
  なる。`gallery.py` が `NL = chr(10)` を使っているのは同じ理由
- **絵が変わったかを mp4 全体の md5 で判断しない。**BGM を敷いた音声は同じ
  入力でもビット単位では再現しないので、絵が1ピクセルも変わっていなくても
  md5 は変わる。比べるのは映像ストリームだけ:
  `ffmpeg -v error -i a.mp4 -map 0:v -c copy -f md5 -`
- 書き出したら**フレームを実際に見る。**タイトルとラベルの重なりは絵を見るまで
  分からなかった（「今日はこ眼鏡橋こに」）
- `readings.json` を直すと指紋が変わり、その回の**全行が録り直し**になる
- 踏んだことは README の「つまずきどころ」か `docs/` に**その場で書き戻す**
